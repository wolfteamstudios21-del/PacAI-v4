{
  "name": "pacai-v4",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd client && npm run build && cd .. && esbuild server/index-prod.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/index.js",
    "dev": "vite"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.3.1",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "cors": "^2.8.5",
    "express-rate-limit": "^7.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
